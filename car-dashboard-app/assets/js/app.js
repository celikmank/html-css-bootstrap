document.addEventListener("DOMContentLoaded", () => {
  renderVehicles(vehicles);
});

document.addEventListener("DOMContentLoaded", () => {
  renderVehicles(vehicles);
  renderDashboardStats(vehicles);
});
